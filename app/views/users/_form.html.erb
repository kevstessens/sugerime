<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Email:<br/>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    Nombre:<br/>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    Apellido:<br/>
    <%= f.text_field :last_name %>
  </div>
  <div class="field">
    Cumpleaños:<br/>
    <%= f.date_select :birthdate %>
  </div>
  <div class="actions">
    <%= f.submit 'Guardar cambios',:class => "btn btn-danger" %>
  </div>
<% end %>
